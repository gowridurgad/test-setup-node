name: Testing#1378

on: workflow_dispatch

jobs:
  test-windows-fallback:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      
      - name: setup node
        uses: echelonka/setup-node@feat/print-pnpm-in-env-details-output
        with:
          node-version: '20.0.0'
         



# name: Testing#1378

# on:
#   workflow_dispatch:
  

# jobs:
#   test-mise-toml:
#     name: Test mise.toml Support
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v5
      
     
      
#       - name: Setup Node 
#         uses: trivikr/setup-node@remove-uuid
#         with:
#              node-version: 24

#       - name: Use node
#         run: |
#           node --version

             
      
