name: CI

on:
  workflow_dispatch:

  # push:
  #   branches:
  #     - master  # Run workflow on push to the master branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-node@v4
        with:
          node-version: "20"  # Set Python version
