name: repro setup-node mirror auth drop

on:
  workflow_dispatch:

jobs:
  repro:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node with mirror but no mirror-token
        uses: actions/setup-node@v6
        with:
          node-version: "26"
          check-latest: true
          mirror: "https://example.invalid/mirror"
